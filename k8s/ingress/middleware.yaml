apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rate-limit
  namespace: portfolio
spec:
  rateLimit:
    average: 4
    period: 1m
    burst: 2
    sourceCriterion:
      ipStrategy:
        depth: 1
